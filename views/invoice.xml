<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2017-22  Kbizsoft
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
    <!-- <record id="view_invoice_form" model="ir.ui.view">
        <field name="name">account.move.form - invoice_fixed_discount</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">

            <xpath expr="//field[@name='invoice_line_ids']" position="before">
                <field name="product_added" invisible="1"/>
            </xpath>
            <xpath expr="//field[@name='invoice_line_ids']" position="attributes">
                <attribute name="attrs">{'readonly':[('product_added','=','True')]}</attribute>
            </xpath>

            <xpath expr="//field[@name='invoice_line_ids']/tree/field[@name='discount']" position="before">
                <field name="discount_fixed"/>
            </xpath>

            <xpath expr="//field[@name='invoice_line_ids']" position="after">
                <field name="discount_amount"/>
            </xpath>

        </field>
    </record> -->
     <!-- <record id="view_order_line_tree" model="ir.ui.view">
        <field name="name">sale.order.line.tree</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
            <tree string="Sales Order Lines" create="false">
                <field name="order_id"/>
                <field name="order_partner_id"/>
                <field name="name"/>
                <field name="salesman_id"/>
                <field name="product_uom_qty" string="Qty"/>
                <field name="qty_delivered"/>
                <field name="qty_invoiced"/>
                <field name="qty_to_invoice"/>
                <field name="product_uom" string="Unit of Measure" groups="uom.group_uom"/>
                <field name="price_subtotal" sum="Total" widget="monetary"/>
                <field name="currency_id" invisible="1"/>
            </tree>
        </field>
    </record> -->

    <!-- <record id="view_sale_order_form_inherite" model="ir.ui.view">
        <field name="name">sale.order.line.form.inherite</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//tree/control" position="after">
                <field name="product_added" invisible="1"/>
            </xpath>
            <xpath expr="//tree/control" position="attributes">
                <attribute name="attrs">{'invisible':[('product_added','=','True')]}</attribute>
            </xpath>
        </field>
    </record>

    <record id="view_sale_form_inherit" model="ir.ui.view">
        <field name="name">sale.order.tree.inherit</field>
        <field name="model">sale.order.line</field>
        <field name="inherit_id" ref="sale.view_order_line_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_id']" position="before">
                <field name="product_added" invisible="1"/>
            </xpath>
            <xpath expr="//field[@name='order_id']" position="attributes">
                <attribute name="attrs">{'readonly':[('product_added','=','True')]}</attribute>
            </xpath>
        </field>
    </record> -->
</odoo>
